import React from 'react';

const Country = ({countries}) => {
    console.log(countries);
    return (
        <div>
            <h1>from Country Component</h1>
        </div>
    );
};

export default Country;